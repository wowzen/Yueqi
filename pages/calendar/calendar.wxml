<!--pages/calendar/calendar.wxml-->
<view class="header">Select the dates for your event</view>
<calendar active-style='square' days-color="{{dayStyle}}" week-style="cn" binddayClick='dayClick'/>

<view class="header">Select the time for your event</view>

<view class="time-picker">
  <picker mode="time" value="{{start_ime}}" start="00:00" end="23:59" bindchange="bindStartTimeChange">
    <view class="picker">
      Start TIme: {{start_time}}
    </view>
    </picker>
</view>

  <view class="time-picker">
  <picker mode="time" value="{{end_time}}" start="{{start_time || '00:00'}}" end="23:59" bindchange="bindEndTimeChange">
    <view class="picker">
      End Time: {{end_time}}
    </view>
  </picker>
</view>

<view> 
  <button bindtap="addSlot">Add Slot</button>
</view>

<view wx:if="{{slotsAdded}}" >Below is your chosen Slots</view>
<view class="chosenSlot"  wx:for="{{chosenSlots}}">
  <view>{{item.start_date}}</view>
  <view>{{item.end_date}}</view>
</view>

<view wx:if="{{slotsAdded}}"> 
  <button bindtap="toShowPage">View your Invitation</button>
</view>

